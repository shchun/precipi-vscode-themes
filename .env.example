# Copy this file to .env and fill in the values.
# .env is gitignored — never commit it.

# Azure DevOps Personal Access Token (scope: Marketplace > Manage,
# organization: All accessible organizations). Used by vsce to publish.
VSCE_PAT=

# Marketplace publisher ID — must match "publisher" in package.json.
VSCE_PUBLISHER=precipi
